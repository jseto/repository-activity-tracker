import { Organization } from '../organization/organization'

export class Repository {
	name: string
	fullName: string
	organization: Organization
	url: string
	description: string
	watchers: string
}